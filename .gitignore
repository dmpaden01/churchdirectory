# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# Dependencies (each package also has its own, this covers the root just in case)
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local
.env.proddb

# Uploaded family/individual photos (runtime data, not source)
backend/uploads/
